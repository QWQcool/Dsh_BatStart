# 2026-08-26

## dsh-trivium 升级到 0.4.9（npm latest）
- `npm view dsh-trivium version` = **0.4.9**（npmjs + npmmirror 镜像均已是 0.4.9；版本线 0.2.0→0.4.9）。
- 本机运行态 `~/.dsh/profiles/web/node_modules/dsh-trivium` 是 **junction → `Desktop/dsh-trivium`** 源码；该 sibling 仓库已提交推送 `3f8d0e7 "Ship 0.4.9..."`(0/0 clean)。所以运行中的服务早已是 0.4.9，无需重新安装。
- 远端 `origin/main` 与本地 `1de260a2` 已 0/0 同步，无可拉取的变更。
- 本仓库（Dsh_BatStart）不 vendored dsh-trivium；仅 `deploy-extra.cjs` 的 `TRIVIUM_FALLBACK` 与两处文档引用了版本号，原值 `0.4.7`。本次将其统一改为 **0.4.9**（离线回退锚点对齐 npm latest）。
- 提交并推送：README.md / 使用指引.md / dsh-extra/deploy-extra.cjs 三处 0.4.7→0.4.9。
- 结论：dsh-trivium 走 npm latest + 本机 junction 源码，Dsh_BatStart 仓库侧只需保持 fallback 锚点最新即可；运行实例无需改动。
