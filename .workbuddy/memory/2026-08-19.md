# 2026-08-19 工作日志

## 检查改动 + 拉取同步远端 4 个提交
- 用户要求检查项目改动并提交到远端。
- 检查:本地工作区 clean(`git status` 无未提交改动);之前所有修复(去 BOM `ea3ec4d`、插件市场 `440d017`、oh-we-need)均已提交推送。
- 发现:本地落后 `origin/main` 4 个提交(另一环境推送,fast-forward 关系、无分叉)。
  提交:`2aa309f`(补全离线插件 package.json) `410099e`(启动脚本健壮化+移除 plugin-market 重复合并) `5f75b2c`(移除桌面端配套插件,修 404) `d43ab22`(端口占用强制重启+默认启用自定义提示词/第三方思考)。
- 用户选"拉取同步":`git pull --ff-only origin main` 成功(fast-forward 至 `d43ab22`)。

## 同步后对齐部署(~/.dsh)
- 停旧服务(PID 208356)。
- 重跑新 `deploy-extra.cjs`:`main()` 已不再调用 `deployPluginPatches()`(注释:zat 现自带 bundle.patch,web profile 再加会 duplicate 崩溃)。
- 清理 `~/.dsh/profiles/web/node_modules` 桌面插件残留(仓库 `5f75b2c` 已移除):`harness-pet` 及 `@deepseek-ai/` 下 dsh-balance/dsh-client-file-changes/dsh-conversation-tweaks/dsh-file-changes/dsh-float-window/dsh-terminal-tab/dsh-wsl-settings。`mv` 备份至 `.workbuddy/tmp/dsh-removed-backup/`。保留 dsh-trivium(不在仓库但必需,有 bundle.patch)。
- 重启服务(加载 .env),PID 226060,HTTP 200,无崩溃。

## 验证结论
- 插件市场(zat-dsh-engine):client.js 返回完整 JS(200),且在 index boot graph → 市场 tab 正常。
- dsh-trivium `/plugins/dsh-trivium/client.js` 404 是**正常的**:纯 host 插件,package.json 无 `dsh.client`、无 `exports["./client"]`、lib 无 client.js,只有 host 端(图记忆内核)。别误判为故障。
- 最终:本地 = `origin/main`(`d43ab22`),working tree clean,无需再提交/推送。

## 注意事项
- 拉取新代码后,仓库移除的插件仍会残留在 `~/.dsh`,必须手动清理(否则旧服务 404)。清理用 `mv` 备份而非直接删,可回滚。
- `curl` 对 chunked 响应的 `size_download` 可能报 0,应用 `curl -i` 看实际 body 判断端点是否真正常。
