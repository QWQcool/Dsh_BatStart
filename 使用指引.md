# DeepSeek Harness — 使用指引(中文)

本目录已安装 **DeepSeek Harness(dsh)开发者预览版 v0.1.0-rc.6**。
**当前状态:环境已就绪,DeepSeek API Key 已配置,可直接使用。**

> ⚠️ v0.1 为开发者预览版,官方警告后续会有破坏性更新,建议仅用于体验,勿接入核心生产业务。

---

## 一、启动

**双击 `start-dsh.bat`** 即可(脚本已内置 Key、自动用本地 node 拉起,无需联网)。

启动后浏览器会自动打开(或手动访问):**http://127.0.0.1:3080**

> 说明:`dsh` 会把启动目录当作默认文件系统位置,即本目录
> `C:\Users\Administrator\Desktop\DeepSeek_Harness`。

也可在已装好 node 的终端手动启动:

```sh
# 确保已设置环境变量 DEEPSEEK_API_KEY(本机已 setx 持久化)
# 然后用本地安装直接拉起:
node node_modules/@deepseek-ai/dsh/lib/bin.js web
```

---

## 二、API Key 配置说明(已完成)

- Key 已通过两种方式配置,任选其一即可生效:
  1. **用户环境变量** `DEEPSEEK_API_KEY`(已用 `setx` 持久化,重开终端/双击脚本都有效);
  2. **`start-dsh.bat` 内已内置**同一 Key。
- dsh 的 DeepSeek 插件默认 base URL 为 `https://api.deepseek.com`,
  默认读取 `DEEPSEEK_API_KEY`,因此**只设 Key 就能直接对话**,无需改其他配置。
- 想换 Key 或换模型(OpenAI / Kimi / 兼容端点):打开 Web UI → **Settings → Models** 修改,
  保存即生效,**无需重启服务**。

> 安全提示:API Key 目前以明文存在于本机环境变量与 `start-dsh.bat` 中。
> 若你希望更干净,可在 Web UI 里填入 Key 后,把 `start-dsh.bat` 里那行 `set "DEEPSEEK_API_KEY=..."` 删掉,
> 仅保留系统环境变量方式。

---

## 三、选择工作区

点击 **Choose workspace**,添加并选中你想让 Agent 干活的项目目录。
(未选择工作区前,会话输入框不可用。)

## 四、开始第一个任务

在会话框输入,例如:

> Summarize this repository and identify its main packages.

Agent 就能读写工作区文件、执行命令、委派子任务,并维持计划。
涉及敏感操作时,Web UI 会按权限策略先征求你的批准。

---

## 常见问题

| 问题 | 处理 |
|------|------|
| 启动后页面打不开 | 确认 3080 端口未被占用;看终端是否报错 |
| 换 Key / 换模型 | Web UI → Settings → Models 里修改,保存即生效 |
| 想用其他 profile | `node node_modules/@deepseek-ai/dsh/lib/bin.js --profile <名称>` |
| 版本更新 | 8 月 16 日起 DeepSeek API 分时计价、整体上调,留意价格 |
| 预览版不稳定 | v0.1 为开发者预览,勿用于生产业务 |

---

## 其他运行方式(进阶)

```sh
# 查看 CLI 帮助
node node_modules/@deepseek-ai/dsh/lib/bin.js --help

# 单任务模式(答完即退): headless
node node_modules/@deepseek-ai/dsh/lib/bin.js --profile headless "run the tests"

# 终端交互模式: tui
node node_modules/@deepseek-ai/dsh/lib/bin.js --profile tui
```
